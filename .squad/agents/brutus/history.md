# Project Context

- **Owner:** Brian
- **Project:** Ancient Coins — full-stack PWA for managing a personal ancient coin collection
- **Stack:** Go 1.26 / Gin / GORM / SQLite (API), Vue 3 / TypeScript / Pinia / Vite (Frontend), Python 3.12 / FastAPI / LangGraph (Agent), Docker
- **Architecture:** Layered — Handler → Service → Repository → Database. Enforced by architecture_test.go.
- **Created:** 2026-04-24

## Learnings

- **2025-07-18:** Maximus completed comprehensive `docs/ARCHITECTURE.md` covering full-system architecture (Go API, Vue frontend, Python agent service, data flows, DB schema, auth, agent integration, Docker, design decisions). This is the authoritative reference for all agents and team members.

- **2026-04-24:** Full test coverage review completed. Go API: 18 tests pass (2 arch, 9 repo, 7 service), measured coverage 3.5-4.6%. Only CoinRepository and CoinService have tests — 20+ repos, 15+ services, all handlers untested. Python agent: 31 tests pass, covers models/retry/streaming/validation but zero team pipeline tests. Frontend: zero test files, no framework configured, 60+ components untested. Architecture tests enforce no-database-import and no-raw-SQL-in-handlers but not the full import matrix. Auth/middleware is the highest-risk gap. Report written to `.squad/decisions/inbox/brutus-code-review.md` with 25 prioritized backlog items.

- **2026-04-24:** P0 test gap closure completed. Added 34 new tests across 4 files covering the entire auth/security surface: `services/auth_service_test.go` (15 tests: registration, authentication, JWT generation, token rotation, password hashing), `middleware/auth_test.go` (10 tests: JWT valid/missing/malformed/expired, API key valid/invalid/revoked, query param token), `handlers/auth_handler_test.go` (13 tests: register/login/refresh/setup HTTP-level), `handlers/coin_handler_test.go` (11 tests: CRUD with ownership enforcement). All 52 Go tests pass. Test pattern: real in-memory SQLite via glebarez/sqlite, httptest+Gin for handler/middleware tests, real bcrypt for password tests.

- **2026-04-24:** P1 test items completed. (1) Vitest configured for Vue frontend: `vitest.config.ts` with jsdom, globals, `npm run test` / `npm run test:watch` scripts added, 7 passing tests on CollectionPagination component. (2) ValuationParser: 18 table-driven tests covering JSON parse, regex fallback, edge cases (zero/negative/malformed/empty). (3) SettingsService: 10 tests covering get/set/defaults/empty-value behavior using in-memory SQLite. (4) Architecture import matrix test (`TestPackageImportMatrix`) added to `architecture_test.go` — enforces handlers→services/repo/models, services→repo/models, repo→models, models→stdlib-only via AST parsing. All 77 Go tests and 7 Vitest tests pass.

- **2026-04-24:** SettingsService currently uses package-level `settingsDB` global (not constructor injection). Tests save/restore the global. When Cassius completes the DI refactor, settings tests will need updating to use the new constructor pattern.

- **2026-04-24:** P1 #22 and #23 completed. Auth store tests (17 tests in `src/web/src/stores/__tests__/auth.test.ts`): initial state, localStorage persistence/restore, login/register flows, logout clears all state, computed isAuthenticated/isAdmin, double login overwrites, logout-when-logged-out safety, token refresh sync callback. API client tests (24 tests in `src/web/src/api/__tests__/client.test.ts`): sanitizeCoin normalizes empty/undefined→null, preserves 0 and valid numbers, date conversion to RFC3339, immutability; JWT request interceptor; 401 response interceptor (refresh attempt, no-refreshToken clear, non-401 passthrough, no double-retry); 8 API wrapper URL/param correctness tests. All 48 Vitest tests pass, `npm run build` clean. Key pattern: mock axios via `vi.mock` with interceptor capture; mock localStorage via `vi.stubGlobal`; test sanitizeCoin indirectly through createCoin/updateCoin since it's not exported.

<!-- Append new learnings below. Each entry is something lasting about the project. -->

- **2026-05-21:** Auction-ending scheduler now available for testing. Cassius implemented backend (`services/auction_ending_scheduler.go`) with 3 new settings keys (enabled, start-time, interval). Aurelia completed admin panel UI in `AdminSchedulesSection.vue`. Pushover integration allows configurable alerts for upcoming auction ending dates. Ready for E2E test scenarios.

- **2026-05-22:** Completed test coverage for auction ending manual-run and run log features. Created 16 new tests across 2 files: (1) `repository/auction_ending_repository_test.go` — 10 tests covering CreateRun, CompleteRun (success/error), ListRuns (pagination, empty, limit edge cases), GetRunByID (success/not found); (2) `handlers/auction_ending_admin_test.go` — 6 tests covering manual trigger endpoint (admin/user/no-auth), list runs endpoint (admin/pagination/no-auth). All tests pass. Cassius's implementation is clean: model (`AuctionEndingRun`), repository (`AuctionEndingRepository`), handler (`AuctionEndingAdminHandler`), scheduler `RunNow()` method, and wiring in `main.go` all follow established patterns. Test approach mirrors valuation run test pattern (in-memory SQLite, httptest+Gin, JWT middleware mocks). Repository tests verify CRUD, pagination defaults, empty slice vs nil. Handler tests verify auth/admin guards, response shape, pagination query params. No gaps found.

## Team Updates

- **2026-05-22:** Led quality assurance on auction-ending manual-run feature. Validated Cassius's backend implementation (16 comprehensive tests across repository and handler layers covering CRUD, pagination edge cases, error scenarios, and authorization guards). **Verdict: APPROVED** — implementation is production-ready, follows established patterns, no blocking issues. Coordinated with Aurelia on frontend integration; flagged endpoint URL alignment issue for follow-up fixup.


- **2026-05-28:** Constitution v2.0.0 landed today. Read `.specify/memory/constitution.md` before next task. Key changes: (1) §0 Hierarchy of Authority defines 8-tier document precedence (Constitution → PRD → specs → plans → tasks → backlog → decisions → agent judgment), (2) §17 Quality Gate now gates every PR (includes go test/ruff/pytest), (3) §21 Definition of Done is a 14-item checklist in `.github/pull_request_template.md` that you must satisfy on every PR, (4) §18 AI Agent Operating Rules forbids `SESSION-NOTES.md` and `.copilot-state.md` — the Squad handoff surface is `.squad/log/` + per-agent history + `.squad/decisions.md`. Governance scaffolding added: SECURITY.md, CODEOWNERS, issue/PR templates. Architecture tests and coverage targets remain as-is (3.5-4.6% Go coverage, 31+ Python tests); these will be formalized in Phase 3. No test code changes yet — Phase 2 starts with retroactive `specs/001-foundation/` seeding and updated test plan.

- **2026-05-28 (Phase 2 complete):** Phase 2 of tech-inventory alignment landed. `specs/` is now the on-disk home for SpecKit workflow (§0 Hierarchy items 3–6 now have concrete directories). Backlog cards in `specs/_backlog/F001-F007-*.md`, active features in `specs/NNN-slug/`, retroactive anchor in `specs/001-foundation/spec.md` (v1.0 surface documented with plan + shipped tasks). Four new session-protocol prompts available in `.github/prompts/`: `/load-context` (cold-start), `/checkpoint` (mid-session pause), `/handoff` (end-of-session merge), `/audit` (§20 audit). Use `/speckit.specify`, `/speckit.plan`, `/speckit.tasks` for new features. Manifest sync deferred — run `specify upgrade` to register prompts. Phase 3 (PRD, ADRs, threat-model split, openapi.yaml, gitleaks, quality-gate workflows) queued.

- **2026-05-28 (Phase 3a complete):** Phase 3a landed today. docs/prd.md is now the product source of truth (Constitution §0 item #2). Four ADRs in docs/adr/ retroactively document v1.0 architecture (0001: ADR practice itself, 0002: three-service architecture, 0003: JWT+refresh+WebAuthn, 0004: design token system). README trimmed 368→90 lines (now setup/links only; consult docs/prd.md for product detail). Any new material design choice requires an ADR per §22.

- **2026-05-28 (Phase 3b testing strategy):** Created `docs/testing.md` as the canonical testing strategy. Current verified surface: Go API has 15 test files / 118 tests total (`src/api/architecture_test.go` = 3 structural tests; 14 handler/middleware/repository/service files = 115 behavior tests), Vue web has 8 Vitest files / 61 tests plus `npm run lint`, `npx vue-tsc --noEmit`, and `npm run build`, and the Python agent has 6 pytest files / 35 tests plus Ruff with no `mypy`/`pyright` config. Biggest gaps: no browser E2E suite, no dedicated Go cross-process integration suite, and no Python static type checker. Best exemplars for future contributors: `src/api/handlers/auth_handler_test.go`, `src/api/services/valuation_parser_test.go`, `src/web/src/api/__tests__/client.test.ts`, `src/web/src/stores/__tests__/auth.test.ts`, `src/agent/tests/test_api.py`, and `src/agent/tests/test_availability.py`. The current CI workflow (`.github/workflows/ci.yml`) runs Go build/vet/architecture checks, Vue `vue-tsc --noEmit`, and Python Ruff + pytest, so local full-suite runs remain important.

- **2026-05-28 (Phase 3b complete):** Operational scaffolding phase landed. Delivered: comprehensive `docs/testing.md` (test audit across all three platforms + exemplars + strategy), and Decision #16 proposal for F011 browser E2E smoke-tests backlog card (Playwright, minimal scope, deterministic fixtures, wired into post-unit-test Quality Gate stage). Identified three lasting gaps: no browser E2E harness, no Go cross-process integration suite, no Python static type checker. Collaborated with Cassius (CI gate) and Maximus (security split); Scribe merged all decisions. Ready for Issue #163 (next coding phase will prioritize security audit before E2E expansion).

- **2026-05-28 (Issue #206 QA audit complete):** Independently audited threat-model.md against current code. Verified three mitigations: (1) F-1/F-2 XSS findings — DOMPurify sanitization confirmed in CoinAIAnalysis.vue, FeaturedCoinModal.vue, useCoinSearchChat.ts (not just claimed); (2) B-2 SQL injection — whitelist map protection verified in analysis.go lines 229–238 and 175–185; (3) SC-2 JWT secret — env-based injection verified in Taskfile.yml and config.go, no hardcoded secrets found. Applied 5 corrective edits to threat-model.md: reclassified F-1, F-2, B-2, SC-2 from Open→Mitigated with concrete code citations; updated status table counts (Mitigated: 4→8, Open: 19→15). Identified blocking issue: zero of 15 open findings have GitHub issue links for execution tracking (blocking criterion from #206 acceptance). Decision recorded in inbox for issue-link design.

- **2026-05-29 (cross-system QA/design-gap audit):** Audited web↔api↔agent integration and timing paths. High-risk gaps found: (1) chat streaming bypasses axios refresh flow (expired JWT fails hard on `/api/agent/chat`), (2) no timeout/cancellation/final-event guard in streaming pipeline (`client.ts` fetch + `agent_proxy.go` timeout=0) can leave UI stuck loading on partial stream failure, (3) availability escalation violates agent contract (`MAX_AVAILABILITY_ITEMS=10`) because Go sends all ambiguous URLs in one request with no chunking, (4) schedulers allow manual+scheduled overlap without run mutex/DB lock, risking duplicate notifications/runs, and (5) security mitigations documented as “Mitigated” (DOMPurify/rate-limit) lack direct regression assertions in tests. Wrote team decision proposal in `.squad/decisions/inbox/brutus-gpt53-audit.md`.

- **2026-05-30 (Collection Health Scorecard #208 test coverage — MERGED):** Delivered comprehensive test coverage for Collection Health Scorecard v1 backend. Created 54 new tests across 4 files: (1) `repository/health_repository_test.go` — 16 tests covering snapshot upsert/update (insert, update with existing ID verification), baseline retrieval (found, not found, closest-before logic), eligible coins listing (active-only filter, user scoping), pagination (page/limit defaults, bounds checking, empty results), and user listing with eligible coins (multiple users, wishlist/sold exclusion); (2) `services/health_service_test.go` — 13 tests covering grade threshold boundaries (A/B/C/D/F at 90/80/70/60), score clamping (-10 to 150 range), default health weights validation (40/20/20/20), collection health summary (empty collection, with coins, trend unavailable), snapshot persistence, coin health listing (empty, with coins, pagination), admin aggregate metrics (empty system, with coins), and fixture builders; (3) `handlers/health_handler_test.go` — 18 tests covering collection summary endpoint (success, empty collection), coin health list endpoint (pagination, invalid params, scope validation all/needs_attention, empty collection, default params); (4) `handlers/admin_health_handler_test.go` — 7 tests covering admin summary endpoint (success, empty system, forbidden for non-admin, response structure validation, multi-user aggregation, wishlist/sold exclusion). All 54 tests pass. Key decisions: (a) in-memory SQLite pattern matches existing tests (`auth_handler_test.go`, `coin_repository_test.go`), (b) tests validate contracts (request/response shape, deterministic scoring, pagination boundaries, auth guards) not implementation details, (c) upsert behavior verified via same-ID assertion after update (implementation uses First→Create or Updates), (d) added `CoinImage` migration to all test DBs to fix subselect queries for image counts. Covered spec requirements FR-001 through FR-010. Frontend tests deferred to component implementation phase (acceptable per task constraints). Blocking gaps: none. Non-blocking recommendations: add scheduler tests (`collection_health_scheduler_test.go`) following existing `auction_ending_scheduler_test.go` pattern. Verdict: backend test coverage production-ready. Merged to `.squad/decisions.md` Decision #18.

  **Cross-agent note:** Validated Cassius's backend implementation (scoring logic, snapshot upsert pattern, scheduler, API endpoints). All architecture rules respected (Principle I layering enforced, no forbidden imports, handlers/services/repos properly separated). Feature ready for frontend integration. Coordinated with Scribe for session log and decision merge. Orchestration log: `2026-05-30T14-02-24Z-brutus.md`.

- **2026-05-31 (Feature #219 QA review — APPROVED):** Reviewed coin detail page refinements implementation. All 12 functional requirements satisfied. Route wiring correct for 4 new section pages (`/coin/:id/{journal,notes,actions,analysis}`) with auth guards. Production build passes (5.75s), vue-tsc type-check clean. No preview/mock artifacts remain. Architecture: shared `CoinDetailSectionPageShell` + `useCoinDetailContext` composable provides consistent section page pattern. Sections properly code-split via lazy route imports. Pre-existing test failures (3 design-token budget + 1 client test) unrelated to feature. Nice-to-haves flagged: dead journalEntries ref in ActionsPage, minor commit hygiene (backup file added/removed across commits). Decision recorded in `.squad/decisions/inbox/brutus-feature-219-review.md`.

- **2026-05-31:** Feature #219 comprehensive QA validation completed. Verdict: **APPROVE**. Full validation scope: (1) Build & type safety — `npx vue-tsc --noEmit` ✅, `npm run build` ✅ (5.75s, no errors), (2) Route wiring — 4 new authenticated subroutes verified working with coin context, (3) FR checklist — all 12 functional requirements (FR-001 through FR-012) confirmed met, (4) Regression testing — 61/65 Go tests pass (4 pre-existing failures unrelated), existing coin detail operations unbroken, console clean. Non-blocking nits: dead code in CoinDetailActionsPage.vue (`journalEntries` ref unused), pre-existing +1 design token budget violation. Feature is ship-ready for merge to main.

- **2026-05-31 (Feature #219 TLC Refinements):** Validated post-merge refinements on #219. Aurelia completed 5 TLC items from Brian's screenshot review (removes duplicate "Actions" heading from panel, removes duplicate category badge, images side-by-side, adds Details heading, confirms no changes to reference section). Follow-up commit (70bd409) applied same deduplication pattern to CoinActivityJournal and CoinAIAnalysis. All changes pass `npm run lint` and `npm run build` clean. Type-check zero errors. Verified original 12 functional requirements remain satisfied. No regressions. Feature remains ship-ready for merge to main.

- **2026-05-31:** Feature #216 Principle V design token remediation under Strict Lockout. Added 12 new design tokens to `src/web/src/assets/styles/variables.css` (--overlay-dark, --overlay-full, --error-bg, --text-warning, --border-white-dim, --shadow-gold-soft, --shadow-gold-hover, --accent-gold-focus, --confidence-high, --confidence-medium, --confidence-low) and replaced all 14 hardcoded color instances in `src/web/src/pages/AddCoinPage.vue`. Maximus issued BLOCK on Aurelia's camera-first implementation for Constitution Principle V violations. Per Strict Lockout §18.2, Aurelia locked out from revision; I (Brutus) executed remediation independently from Maximus's exact specification. Quality Gate: `npm run lint` ✅ (0 errors, 5 pre-existing warnings unrelated), `npm run build` ✅ (8.57s, vue-tsc + vite clean). All flagged violations resolved. Verified visual equivalence — token values equal hardcoded colors they replaced. Changes left uncommitted for Maximus re-review per instructions.

- **2026-06-01 (Issue #218 Polish-phase validation — APPROVED):** Completed T027-T031 validation tasks for external tool server adapter. (T027) Created `src/api/middleware/capability_test.go` with 10 comprehensive tests covering read/write capability scoping, empty/missing/non-string capabilities, invalid scope, and handler execution guards — all tests pass (23/23 middleware tests). Context key verified: `apiKeyCapabilities`. (T029) Go build/vet/test: clean pass across all packages (7 packages with tests, 0 errors). (T030) Frontend build/lint: `npm run build` clean (6.73s), `npm run lint` clean (0 errors, 5 pre-existing warnings unrelated to #218). (T031) Quickstart scenario traceability: Scenario A (read tools) = PASS (4 routes wired with RequireCapability("read"), handlers delegate to CollectionToolsService with userID scoping); Scenario B (two-phase write) = PASS (propose + commit routes wired with RequireCapability("write"), journal source `external_tool_server` with API key metadata threaded via CommitUpdateExternal); Scenario C (OpenAPI discovery) = PASS (GET /api/v1/tools/openapi.json served via embedded YAML, kill-switch gated but no auth). Negative scenarios N1-N6 = all PASS (N1: read-key → write denied by middleware, N2: cross-user denied by service-layer OwnedBy scoping, N3: non-allowlisted field denied by service, N4: token replay/expiry denied by proposal state check, N5: kill-switch off → 503 via ExternalToolServerEnabled middleware, N6: rate-limit per-key via ExternalAPIKeyRateLimit). Success criteria SC-001–SC-007: all satisfied in code (SC-001/SC-002: userID scoping in service layer, SC-003: two-phase token flow enforced, SC-004: journal source + API key metadata, SC-005: capability middleware guards write routes, SC-006: kill-switch gate on all routes, SC-007: OpenAPI served). Manual runtime steps documented: server start, admin toggle on, create read/write keys, run curl scenarios A/B/C + N1-N6, client discovery (OpenWebUI/LibreChat/n8n/mcpo). **Verdict: APPROVED** — #218 backend implementation production-ready, all polished-phase validations pass.

- **2026-06-01 (Issue #218 BLOCK resolution — Strict Lockout):** Fixed BLOCKING type assertion panic risk in `src/api/handlers/external_tools.go` per Maximus review finding (§18.2 Strict Lockout — Cassius blocked, Brutus owns fix). Applied comma-ok defensive pattern to ALL six handlers: (1) SearchMyCollection (line 53: userID.(uint) → guarded with 401), (2) GetCoin (line 91: userID.(uint) → guarded with 401), (3) CollectionSummary (line 124: userID.(uint) → guarded with 401), (4) TopCoinsByValue (line 161: userID.(uint) → guarded with 401), (5) ProposeUpdate (line 199: userID.(uint) → guarded with 401), (6) CommitUpdate (lines 253, 257-259: userID.(uint), apiKeyID.(uint), apiKeyName.(string), apiKeyCap.(string) → all guarded; userID missing/wrong type returns 401 "Unauthorized", API key context missing/wrong type returns 403 "Insufficient capability"). Pattern: `val, exists := c.Get("key"); if !exists { ... }; valTyped, ok := val.(Type); if !ok { ... }`. Eliminates panic risk if middleware chain bypassed, reordered, or auth behavior changes. Preserves all success-path behavior unchanged. Quality Gate: `go build ./... && go vet ./... && go test ./...` all PASS (7 packages with tests, 0 errors). Defensive coding pattern applies to all Gin context value extraction — never assume middleware-set types are guaranteed without type guard.
